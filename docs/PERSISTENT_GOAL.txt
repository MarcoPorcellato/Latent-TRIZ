Current A0X lifecycle, hashes, and active boundary: see the sole ledger in
`docs/A0X_SIX_MODEL_CAMPAIGN.md`. Architecture authority:
`docs/A0X_ARCHITECTURE_CONVERGENCE.md`. Earlier A0X checkpoint blocks below are
historical.

--- Historical checkpoint below; retained with original hashes and outcomes ---

A0X Hosted Gate A local implementation checkpoint (2026-08-31): resume only
from isolated branch `agent/a0x-hosted-gate-a-design`. Tasks 1–9 are complete;
Task 9's stale package refusal is Historical evidence and Task 10 alone may
regenerate. Verify branch, HEAD, ancestry, tree, and dirty state before acting.
Canonical specification:
`docs/superpowers/specs/2026-08-31-a0x-hosted-gate-a-design.md`.
Canonical implementation plan:
`docs/superpowers/plans/2026-08-31-a0x-hosted-gate-a-implementation.md`.
Terra handoff: `docs/A0X_HOSTED_GATE_A_TERRA_HANDOFF.md`.
Sol architecture/security review:
`docs/A0X_HOSTED_GATE_A_SOL_ARCHITECTURE_REVIEW.md`.

Hosted Gate A has seven lanes: `repository-python311`,
`schema-cross-validation-python311`, `repository-python312`,
`schema-cross-validation-python312`, `a0x-no-model`, `a0x-synthetic`, and
`documentation-audit`. It accepts four hosted inputs (32 KiB manifest, 1 MiB
bundle, 2 MiB trusted root, 16 KiB transport) and Gate B creates the fifth
verification receipt (32 KiB). No rerun and no CCP Gate A fallback are allowed;
CCP Gate C remains independent. The first real post-merge hosted Gate A run is
acceptance only. The trusted-root snapshot cannot identify revocations
published after that snapshot. Capture, publication, Gate B, and Gate C each
need separate authorization.

Execute Tasks 10–11 in order under the frozen Sol rulings. Terra owns
integration; Luna distills inventories, determinism and documentation checks.
Do not weaken or regenerate around an intermediate failure. Task 10 alone
regenerates two inventories, two freezes, twelve dossiers, and the no-model
receipt from its recorded implementation anchor.

Stop before Task 12. Push, PR, merge, hosted-run acceptance, evidence capture,
offline verification of real hosted bytes, publication, Gate B/C, model,
tokenizer, target, CCP heavy work, Docker, scientific execution, retry, and
claim promotion require their own exact authorization. Preserve historical
packages and receipts byte-identically. Continue until Task 11 produces a clean
exact-head local candidate and publication dossier, then request Sol/operator
review.

--- Historical checkpoint below; retained as evidence, not current authority ---

A0X post-bootstrap hosted-integration checkpoint (2026-08-30): verify live
state before acting. Public `main`
`d2a475f58db668a2ce0a4ec48082189422b19eab`, tree
`4d2b1221dd63a89d6c6c4433061a7d8ed130b76e`, merged PR #110 and now installs
the pinned schema oracle in both hosted repository lanes. PR #110 used a
one-time administrative CCP-backed bridge after its hosted run failed on the
missing oracle; that bridge is not a GitHub-hosted PASS and must not be reused.
PR #109 is reconstructed without history rewrite by integration ancestry commit
`7ac5a6065d78974f52a86816b019184f8f147bd7`; its tree remained byte-identical
to the pre-integration A0X head because the bootstrap delta was already present.

The Gate B hardening implementation remains anchored at
`74d6bc048e656f3ced2d4bc6db4b0492dfd16359`; regenerated bindings remain at
`50cf959e7a9b50d68ee58a11ac063e6681761abe`. Material contract
`b56b860a4f4673f675035e0c76aa1b79e75b37ace9c441b2d1e36076d35c3fc8`
and A0/A0-R1 freeze hashes
`7b4920328414ae93eda793b00770ca1dae080656bf62600b233e8c1afd6448ff` /
`9713376406522581cec9c32cc71f0e4c215066e47fe875e4c332ee49ff8b00e1`
remain unchanged. Reverify the target-free package, push PR #109 non-forced,
and require fresh GitHub-hosted Python 3.11/3.12, scientific audit, trusted
classification, and aggregate gate evidence on one unchanged exact head.
Stop before merge unless it is separately authorized. Do not prepare Gate B
assets, load models/tokenizers, open targets, execute Gate C, or promote claims.
The state remains `sealed_gate_pending`.

**Historical pre-bootstrap publication checkpoint.** Public `main`
`78b40677d7cd8b58421a6a2a80cb6feb066f85b3` restored the first hosted lanes
through PR #108. Its prior test counts and next-step wording are superseded by
the post-bootstrap checkpoint above; the underlying receipts remain historical
evidence.

**Historical pre-material readiness checkpoint (superseded).** Exact source
`68f8bfe75a883054118246101485f71a56a5e82e` passed Gate A and its receipt is
publicly preserved (receipt-file SHA-256
`3f75c665115c00fd18df1a5fb403f6dd5e410b5d5cdb12c78eada39effb1810e`,
evidence commit `fc46c39421ae85713f473ef49a1270beab3aefe6`). Later implementation hardening
made that receipt historical; it does not authorize the current branch.

A0X Gate A temp-mount correction checkpoint (2026-08-29): the campaign remains
`sealed_gate_pending`. The exact-head qualification at
`e340e142fcd745d47dec1df386eb9fdb1b2e15f7` ended terminal `FAIL`; both schema
checks passed and both repository checks returned exit code 1 without timeout.
Receipt-file SHA-256:
`6e354744099921f240108698258a184b2bdfbe170e9b29975bb305a88cfb99ac`.
One authorized Python 3.11 diagnostic established a single test-fixture cause:
the repository check selected writable `/dev/shm`, but inert synthetic CCP and
Python files created there did not satisfy executable access in the container.
The production fail-closed executable validator remains unchanged. A TDD
regression now makes only those two exact synthetic files independent of the
temporary mount's executable policy and delegates all other access decisions
to the real operating-system probe.

Because `tests/test_a0x_runtime_bundle.py` belongs to both frozen
implementation inventories, the correction and canonical notes were committed
at implementation anchor `d4845f0a7b204ba65b9669c05a677fc0560ababd`.
Deterministic regeneration completed two implementations, two freezes, and all
twelve dossiers with zero CCP, model, tokenizer, target, or remote access.
A0 implementation/freeze SHA-256 values are
`2398f026dc352be8a11950e0cb0996437d87b4ca1f0db11558d40e16f31c7b57` and
`cc78b1baf158d0a0c3f9e77cd411d8fff5abd0b579947687c2f53d55aa027ac1`;
A0-R1 implementation/freeze values are
`6246c84fc4c7fc48114598406c5fa6a8b457f2fdb973626142bad30e7c68e004` and
`c4564adcd1e767e339467db953540123017284461abbd8225ed95ab1bb49695a`.
Frozen verification passed 11/11, the synthetic aggregate passed 268/268, and
the repository check passed 1,100 tests with 11 documented skips. Commit the
regenerated package and final notes, then stop for a new explicit Gate A
exact-head authorization. Do not reuse or relabel the failed receipt.
No CCP heavy command, Docker diagnostic, model, tokenizer, target, runtime
bundle, material run, network, publication, or retry is authorized by this
checkpoint.

A0X runtime-binding correction checkpoint (2026-08-29): the campaign is
`sealed_gate_pending`. Descriptor-v2 is acyclic: the operator-rooted
authorization binds exact descriptor bytes, while the descriptor carries only
the pair-derived authorization path and byte-bound material contract. The
target-free preparer writes readiness, descriptor, authorization, and mapping
in that order and refuses every overwrite. Its implementation inventory must be frozen
before the two leg freezes and twelve approval-request dossiers are regenerated
from the resulting exact implementation HEAD.

The campaign has three explicit operator stops, which are not interchangeable:
**A** authorizes one exact-head repository qualification; **B** separately
authorizes preparation of one exact pair/attempt private runtime bundle; and
**C** later authorizes one one-shot material attempt bound to the prepared
authorization raw SHA-256. The current work stops before **A**. It cannot
prepare a private bundle under **B**, and no prepared bundle authorizes **C**.
`sealed_gate_pending` remains the only current execution state.

The old local qualification of `4aee4698f5c59101b1f3292519f10ae802629bf7`
and its receipt-file SHA-256
`08b1a8f1c08d2ab9784c95acd3b452c218b76108744a129cd6b8df2aef52c447` are
historical only. They cannot attest this corrected source. Stop after the
target-free package regeneration and no-model verification for one new,
explicit exact-head repository-qualification authorization (**A**); only a
subsequent separate exact pair/attempt bundle-preparation authorization (**B**)
may name a private runtime bundle. A later one-shot material authorization
(**C**) must bind the prepared authorization raw SHA-256. No model, tokenizer,
target, CCP heavy command, Docker, network, publication, scientific retry, or
claim promotion is authorized before its respective stop.

**Historical A0X runtime-binding correction state (2026-08-29; superseded).** The exact
implementation anchor is `e2f557909c9f816eb40ae5aae7be54cb523c97cd`. The
generated package commit is
`a52a621d67a379937475fa066639cedd215c4c27`; it precedes this
documentation-only clarification. The then-current A0 freeze SHA-256 was
`a8ca1889f91cd965399eaa9f3ac066d1f5b7bc9beea26ee2ceca517ebb358353`, and
the then-current A0-R1 freeze SHA-256 was
`b8c470d45b098233f3497cb75d7fc95a41edd9f7772539b55db71aa803355c64`.
This documentation-only commit is not in either implementation inventory, so
it does not alter that anchor or authorize regeneration. The stop boundary is
unchanged: **A** is one new explicit exact-head repository qualification; **B**
is a separate exact pair/attempt bundle-preparation authorization; and **C** is
a later one-shot material authorization bound to the prepared authorization raw
SHA-256. The current work stops before **A**; `sealed_gate_pending` remains the
only current A0X execution state.

**Historical A0X checkpoint (superseded; not the current runtime-binding
state).** Preparatory work previously remained on branch
`agent/a0x-six-model-design` at `sealed_gate_pending`, with correction anchor
`6b8c8e3491b24fa4717b2f4faa8700b007c48892`, tree
`18b8fdaf9ba00a81e3c90686a2563a23f2436824`. The public material contract
deliberately binds the large-blob-qualified CCP producer at commit
`27adf8d0820b3cd96f9c5e149de9b580ae41f639`, tree
`d8e0364d1313fde0898a44517ae6d233d9e10763`, executable SHA-256
`c8021e2322e172686c0a0c07d2b0260eafb5812d085d2306dbbde3fe4e964bd4`.
Its authorized Matrix qualification passed all required checks. The
receipt ID is
`sha256:21d5cf99a9d142b879b37ef8bb2f50573e45fd569a2259fa863a50fe6be08e85`
and the receipt-file SHA-256 is
`14df36450ce982b0c5233651baa4c5f5d0e0c462b1b5f119ec8f93a9ad7465ce`.
Both candidate and installed-stable verifiers returned PASS. The source is
preserved on public branch `agent/matrix-v2-legacy-terminal-release-qualified`,
the receipt only on its exact evidence branch, and CCP PR #70 is merged as
`1a2e081cd3912b0fd63a7226a4564f1d85a51eb8`, whose tree is byte-identical to
the qualified source tree. The candidate is not installed and has not replaced
the stable executable.

The fixed material envelope is 3,600 seconds outer, 3,300 seconds internal,
300 seconds reserved for sealing/cleanup, and 300 seconds for admission. The
scientific workload uses one `guard exec`; its fresh six-role preflight does
not call configuration-backed `plan`, `doctor`, or `dry-run`. Public artifacts
contain no host paths, argv logs, environment, usernames, cache paths, or
container IDs. Private runtime inlets are deterministic per leg/model/run and
remain outside immutable result directories.

The single authorized repository qualification of
`32e03b5ef34bb1d8f778877514601994df9c3898` ended terminal `FAIL`: both schema
checks passed, while both repository checks reached their approximately
300-second configured deadlines. Receipt ID
`sha256:bbe9173bfe489e34071f71ce6822df26126f1026d939e1693245fd47daa864d9`;
file SHA-256
`63a920e8cd97310a857be8465924311389edeb61746945c9219f4c85e2500e01`.
Correct V2 verification established receipt integrity `PASS` and policy
qualification `FAIL`; an earlier V1-policy verification was an operator
invocation error. The correction sets the two repository checks to 3,600
seconds, retains 300 seconds for schema checks, requires
`--matrix-plan-profile matrix-v2-legacy-v1` on every repository-qualification
target, and verifies with `.commit-ci-policy-v2.toml`. No retry occurred.

The next single authorized qualification at
`fb9484a89549fbbbfc5395932954b2d9565d91d6`, tree
`f0585981d136659df4fec39e8b26aaaf2fab02a3`, also ended terminal `FAIL`.
Both schema checks passed; both repository checks returned exit 1 without
timeout or cancellation. Receipt ID
`sha256:f5348d82568ba98c6003132534b3a202631f04c42972b965251adaa2ca367dde`;
receipt-file SHA-256
`5bb2e49da31381e4c22858556e4c54f373ee69dfcea8f578e050efb6268e4232`.
Correct V2 verification established receipt integrity `PASS` and policy
qualification `FAIL`. A clean host run passed all 1,075 repository tests,
while the exact Matrix test reproduced five `FileNotFoundError: 'make'`
errors with `make` absent. The verification images remove build-only `make`
during final package cleanup. TDD therefore replaced every `make -n` test
subprocess with dependency-free Python inspection that requires one exact,
non-empty recipe for each of `preflight-plan`, `preflight-doctor`,
`preflight-dry-run`, `preflight-run`, and `preflight-verify`. The attempt is
consumed and has not been retried.

The generator produced two frozen legs and twelve `approval_requested`
dossiers with zero material access. The active checkout regenerated the
package deterministically, and the frozen verifier independently repeated its
byte-determinism checks in disposable source copies. Fresh verification passed:
the A0X aggregate 248 tests, the frozen package 10/10, schema cross-validation
155 agreements with 19 rejected mutations, and the repository-wide suite
1,075 tests with one documented skip. The
no-model receipt SHA-256 is
`c761ae76d77b976ea83bc83aa139da9730858a387422aff501ffad1b87217e4c`.
The receipt reports zero model loads, tokenizer constructions, sealed-target
reads, CCP invocations, and remote mutations. Resume through
`docs/A0X_SIX_MODEL_CAMPAIGN.md`, `docs/A0X_RESTART_HANDOFF.md`,
`docs/A0X_ENGINEERING_PROBLEM_SOLUTION_LOG.md`, and the canonical A0X
design/plan.

Historical hashes from the superseded checkpoint: material contract
`b56b860a4f4673f675035e0c76aa1b79e75b37ace9c441b2d1e36076d35c3fc8`,
A0 implementation
`7645761ad5fb7ff42a603a8370bce0be1f3c3f179f937e551194bc2b78f44570`,
A0 freeze
`34876ec4ad5ae209bc3ffd49202deb660830fa225caa00831f70dedfa34bf006`,
A0-R1 implementation
`d6cc0f6de81d0da4108570559579d76045a7a7a1ea282a84866e3825bcb023bc`,
and A0-R1 freeze
`1c7425d90be524ba9ab55ec66967b0eee32272addc025948459ea4e3b6383e8a`.
The reviewed legacy plan has stdout SHA-256
`0969a1eeb62b2a92593cda0b75c8814d7eca893bebc736ec968f02aa9f2a5fad`,
outer digest
`sha256:8eb0172c30aac8f9b47f65cebd222ee6615b17e4053a5a16e2be5583f3a10331`,
Python 3.11 digest
`sha256:aa69a8795e20733a516fac99b253cfc26a9f963825ff1fa9ca5638364f7fc943`,
and Python 3.12 digest
`sha256:072e50972a02f2df710bf81620ca058d230f0637bcc16a47ba35562fe1358510`.

Historical dependency-free verification at that superseded checkpoint passed:
no-`make` regression 3/3, frozen
package 10/10, A0X aggregate 248/248, schema cross-validation 155 agreements
with 19 rejected mutations, repository suite 1,075 tests with one documented
skip, documentation audit, and diff check. Independent Luna review returned
`APPROVE` with no P0--P3 findings. That checkpoint's proposed package commit
and next gate were superseded by the current correction state above.
Installation, each of the twelve
dossier-bound scientific attempts, Latent-TRIZ publication, and merge remain
separate decisions. No
model, material tokenizer, historical or sealed target, CCP heavy command,
Docker, network, GitHub action, retry, tuning, pooling, or claim promotion is
authorized by this checkpoint. The earlier `c91915a…` PENDING receipt and the
failed `a73ebed…` receipt remain historical evidence only.

EXP-002 checkpoint (2026-08-20): branch `exp002-qwen3-followup` is the active
implementation branch at implementation checkpoint `e47937c` (followed by
documentation-only checkpoint commits). The no-model protocol,
seven-model identity registry, target-free 351-question TRIZ bank,
tokenizer-audit gate, public label-surface diagnostic, injected material
runner, immutable publication contracts, guarded EXP-002A CLI, direct-answer
review gate, direct-question and label-free candidate paths, locator-only
source-familiarity boundary, deterministic EXP-002C power calibration,
target-free corpus validator, and the `not_ready` sealed transfer target-key
contract are frozen;
the seven-model EXP-002A baseline is published with terminal `null` packages.
The exact seven-model material dossier is operator-authorized at approval hash
`0c5943ad5a7bf2c598511b8c3ecc29bd566f33140af59c8c6d788f2423483d67`.
The current official CCP source and coordination runbook are anchored at
`origin/main` `3fccc197e5055a2759ee7afe51b91133938ec904`; the separately qualified
installed executable SHA-256 is
`b8d26013800c99ba806506a0539a9ddc781bfab52f95c8f1dbdff1b65c2fcd4c`.
All future heavy work must use the fresh resource/admission/runtime preflight,
non-reentrant slot ownership, and explicit terminal handoff documented in
`docs/reference/commit-ci-preflight.md`. The next objective is to freeze and execute the
remaining response-surface, direct TRIZ/source-familiarity, and new blinded
transfer stages; do not rerun the seven baseline packages.

Complete the canonical improvement plan in `docs/LABORATORY_MASTER_PLAN.md`,
using `docs/HYPOTHESES_AND_FALSIFICATION.md` as the normative Weak/Strong
contract. The current public checkpoint is `main` at
`0123ce467408becbf127b66da1fcd4166bbbd431`; verify `origin/main`, the active
ruleset, open pull requests, and receipt branches live before the next
publication or mutation.

Treat A0/R1 positives, the R3 reference-integrated package, and the seven-model
comparative null packages as immutable exploratory
evidence. Do not rerun or tune them. The immediate priority is the real-human
H1 gate: three independent TRIZ experts and immutable raw/agreement,
abstention, disagreement, adjudication, and keep/amend receipts. Then freeze
stronger lexical, template, source, near-neighbour, generic, cosmetic,
Matrix-direction, unsupported-edge, abstention, random-label, and extreme
domain-shift controls; demonstrate an out-of-sample direction; and prepare one
validated-principle Lab 06 causal dossier. Defer composition and broad model
scaling until that causal gate. A separate no-download dossier now proposes
GPT-2 and SmolLM2-135M are now completed one-run architecture/scale controls;
their terminal outcomes are null, non-pooled, and not a general TRIZ claim.
The official-source runtime/tokenizer contract and the pre-access failure
prevention checklist are maintained in docs/EXP001_ADDITIONAL_MODEL_RUNTIME.md;
SmolLM2-135M is Llama architecture but declares GPT2Tokenizer.
Keep Track B as a separate no-TRIZ-term
from-scratch protocol with independent seeds and checkpoints. Its no-model
contract is now recorded under `experiments/track-b-emergence/`; training and
corpus access remain forbidden until a separate exact dossier and approval.

Do not load models, read sealed targets, train models, or run CCP until the
relevant no-model contract, hashes, resource envelope, and explicit operator
approval are complete. Publish every positive, null, failed, or
non-interpretable result; preserve historical artifacts; use deterministic
tools first; delegate bounded inventory, tests, and documentation to cheaper
workers; require a no-model contributor quickstart; and keep architecture,
statistics, security, CCP, publication, and merge decisions under primary
review. The current public closeout is `main`
`0123ce467408becbf127b66da1fcd4166bbbd431`; all comparative execution and
publication gates are complete. Continue only with the human/external H1 gate
or another separately frozen and authorized protocol.

The additional model execution packages are published under
`results/exp001-comparative/` with public locators and hashes for dense assets
retained locally; a fresh clone must reject those missing assets fail-closed.
The earlier R3 SmolLM2 package remains published in main merge
`4cc1c6d862bffc9558b47a5cadd838a2ee22c465` with terminal `null`, one sealed
target read, and no promoted scientific claim; the current objective extends
the comparative proposal without reopening that package.
The exact acquisition/one-run approval for
EleutherAI/gpt-neo-125m@21def0189f5705e2521767faed922f1f15e7d7db and
Qwen/Qwen2.5-0.5B@060db6499f32faf8b98477b0a26969ef7d8b9987 is now fully
consumed and published. It
bound 529,444,041 bytes across eight GPT-Neo files and 999,586,188 bytes across
seven Qwen2.5 files, with 1 GiB and 1.5 GiB ceilings. Preserve all seven
packages and their non-pooled terminal outcomes; do not rerun them.

The official documentation audit is recorded in
`docs/EXP001_MODEL_OFFICIAL_DOC_AUDIT.md`; it is metadata-only and does not
authorize model access. Before any material run, enforce the exact checkpoint
config, the Qwen 32,768 effective context bound, GPT-Neo right-padding rule,
fast-tokenizer boundary, offline/no-generation mode, and receipt fields from
that audit.

Acquisition checkpoint (2026-08-19): the exact authorized GPT-Neo and Qwen2.5
snapshots are integrity-verified. Receipts are
`results/exp001-comparative/preexecution/gpt-neo-125m-integrity-receipt.json`
and `results/exp001-comparative/preexecution/qwen2.5-0.5b-integrity-receipt.json`;
their model SHA-256 values are `52738cbfb54e25a232598242f60ef19ee193d36090b98fe649b10c02724b3521`
and `88c142557820ccad55bb59756bfcfcf891de9cc6202816bd346445188a0ed342`.
No model load, generation, dense output, or sealed-target access had occurred
at the acquisition checkpoint. That checkpoint is now complete; no further
model or target access is authorized by this dossier.

Execution checkpoint (2026-08-19): both one-shot runs completed and are
terminal `null`. GPT-Neo is published in
`results/exp001-comparative/gpt-neo-125m-21def018-gpt-neo-125m-20260819-01`
with p=.6875, 323.899 s, peak RSS 1,858,076,672 B, and dense asset hash
`53222272e786045156d86ad8aaf9ea123bc8e6efa26778d39008809393850c52`.
Qwen2.5 is published in
`results/exp001-comparative/qwen2.5-0.5b-060db649-qwen2.5-0.5b-20260819-01`
with p=.96875, 935.267 s, peak RSS 4,876,828,672 B, and dense asset hash
`577bea7e3161de630e5e9a649c152d301bc01658827a90d26d24eda48c627b3c`.
Each package records one sealed-target read, no network/generation, and empty
claim IDs. Qwen2.5 publication is merged in `main` at
`a081e612feca348b28253ee0cc9e67c24b45ed3a` (PR #97); GPT-Neo publication is
merged at `74926e15fe06a6e41ef1aa0d96e731b70636531a` (PR #96). Fresh-clone
verification passed for both packages with the declared external assets and
rejected both missing and one-byte-mutated assets. No further runs of these
models are authorized by this dossier.

The next scientific gate is human construct validation: three independent TRIZ
experts, blinded ratings, agreement/abstention/disagreement receipts, and a
keep/amend decision. Until that gate is closed, do not scale models, pool
results, or promote any TRIZ claim.

EXP-002-AUTO checkpoint (2026-08-20): an independent no-model, pre-expert
campaign is being prepared under `docs/EXP002_AUTO_PREEXPERT_CAMPAIGN.md` and
`experiments/exp002-auto/`. It freezes AUTO-0 tokenizer audit; AUTO-1 cyclic
and label-free response surfaces over the published 24 records; AUTO-2 178
automatic factual records; AUTO-3 160 source-familiarity formulations;
AUTO-4 an eight-domain procedural proxy; and AUTO-5 all 24 label permutations
for all seven already registered exact models. The combined factual/procedural
key remains unmaterialized and private; the approval dossier is
`approval_requested`. No model, tokenizer, network, CCP material run, or
sealed target is authorized until the exact frozen dossier hash and a new
operator authorization are recorded. Outcomes remain exploratory, claim-free,
and non-substitutive for the H1 expert gate.

Material-boundary checkpoint (2026-08-20): `origin/main` is
`b2c53ba9c4a4e7c71816e3a85e9902f69cbe84eb`. The no-model tranche now has a
synthetic-tested, adapter-injected material boundary in
`src/latent_triz/exp002_auto_material.py`. It streams exact runtime hashes,
requires an authorized hash-bound dossier and a fresh CCP Admit/inactive/
queue-zero receipt before adapter construction, refuses overwrites, records
all terminal statuses, and gives analysis exactly one sealed-key reader. The
private sealed-key schema is tracked separately while the public key template
remains empty and `not_ready`. This checkpoint still authorizes no model,
tokenizer, network, generation, or target access. The next gate is an
exact-head no-model qualification and merge of this boundary, followed by a
new final dossier hash and explicit operator approval; only then may the
seven-model campaign be considered for execution.

Historical EXP-002 final dossier checkpoint (2026-08-20): PR #102 merged at
`9a86bedcc30eed63bd76e0b5d516c64347e478bc`. The public main branch now
contains the exact material-binding dossier and its fail-closed runner. The
canonical dossier SHA-256 is
`d35921d0ac187ba5b9de4787927df7cb2a1211ba34b9370a778d37b5fa016431` and its
status remains `approval_requested` with `operator_approval.granted=false`.
The corresponding CCP exact-head receipt is published on
`ccp-evidence/5f9c72a10f725a5369ddb982cdcdec74f0c8d186` and verified PASS for
Python 3.11 and 3.12. The next and only material gate is explicit operator
approval of that exact dossier, including the seven frozen model revisions,
CPU-float32/no-network/no-generation limits, one analysis-boundary sealed-key
read, and the historical 1,800-second/8-GiB/128-MiB ceilings. This does not
describe A0X, whose current envelope is 3,600 seconds outer, 3,300 seconds
scientific work, and 300 seconds reserved for sealing and cleanup. Until then, no
model, tokenizer, generation, or sealed target access is authorized.

A0X offline Gate B builder checkpoint (2026-09-01): the target-free builder can
later create an independent Python 3.11 environment from an exact full-runtime
manifest and the exact 39-wheel manifest, then APFS-clone one exact model-card
snapshot. Planning executes no external code. Material mode must be explicit
and may execute or install only after the base runtime and all wheels have been
APFS-bound into private attempt-owned paths. The local wheelhouse manifest
SHA-256 is
`fe541aa83b5dbd9770da1f50d2cd88eb192586406398d9cffa5507f9f352ca72`
and binds 150,397,774 bytes. This checkpoint authorizes synthetic fixtures,
tests, documentation, target-free regeneration, and local commits only. Stop
before any real environment, installation, snapshot, Gate B/C, model,
tokenizer, target, network, CCP, push, or publication.

The exact reviewed implementation anchor is
`b7ab6513c759b4f0133b33c8a70c034d8a1f92ba`, tree
`480e96a94a8602a3ba1bb08741a3ecf0fb86cce1`. Target-free regeneration binds
A0 implementation/freeze
`beea84d11b8c3c5b7262991a3f5e951834b81432c2aea0ca71a521a78228c6c3` /
`38bdbb99db20c471e9aeafeb7b961709ce373d2fd8f0801c56655ac550266677`
and A0-R1 implementation/freeze
`6f8f6adeccb067aec4473a3b46c632db400317bcee432e41186c368a419da021` /
`2ec31870102a9219a36df24304748e31da4050514d9e59cfd0c9056adaba3ffa`.
The ordered twelve-dossier ledger SHA-256 is
`c249f94e3d5124c8fb19ebdf873d4bf91eb2dc7eb6c75654e55d9dff58451df0`.
